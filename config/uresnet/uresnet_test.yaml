# Canonical UResNet semantic-segmentation inference configuration.
#
# Set io.loader.dataset.file_keys and model.weight_path for the target run.
# With no checkpoint, the fixed seed provides deterministic initialization for
# the CI regression test.

base:
  world_size: 0
  iterations: 1
  seed: 0
  unwrap: false
  log_dir: logs/uresnet/test
  log_step: 1
  overwrite_log: true

io:
  loader:
    minibatch_size: 2
    shuffle: false
    num_workers: 0
    collate_fn: all
    dataset:
      name: larcv
      file_keys: null
      schema:
        data:
          parser: sparse3d
          sparse_event: sparse3d_pcluster
        seg_label:
          parser: sparse3d
          sparse_event: sparse3d_pcluster_semantics

model:
  name: uresnet
  weight_path: null
  network_input:
    data: data
  loss_input:
    seg_label: seg_label
  modules:
    uresnet:
      num_input: 1
      num_classes: 5
      filters: 32
      depth: 5
      reps: 2
      allow_bias: false
      activation:
        name: lrelu
        negative_slope: 0.33
      norm_layer:
        name: batch_norm
        eps: 0.0001
        momentum: 0.01
    uresnet_loss:
      balance_loss: false
