# Canonical whole-image energy-regression training configuration.

base:
  world_size: 1
  epochs: 25.0
  seed: 0
  unwrap: false
  log_dir: logs/image/energy/train
  log_step: 1

train:
  weight_prefix: weights/image/energy/snapshot
  save_epoch: 1.0
  optimizer:
    name: Adam
    lr: 0.001

io:
  loader:
    minibatch_size: 64
    shuffle: false
    num_workers: 8
    collate_fn: all
    sampler:
      name: random_sequence
      seed: 0
    dataset:
      name: larcv
      file_keys: null
      schema:
        data:
          parser: sparse3d
          sparse_event: sparse3d_pcluster
        labels:
          parser: single_particle_energy
          particle_event: particle_corrected

model:
  name: image
  weight_path: null
  network_input:
    data: data
  loss_input:
    labels: labels
  modules:
    image:
      objects:
        source: image
      encoder:
        name: cnn
        num_input: 1
        spatial_size: 768
        filters: 32
        depth: 5
        reps: 2
        allow_bias: false
        activation:
          name: lrelu
          negative_slope: 0.33
        norm_layer:
          name: batch_norm
          eps: 0.0001
          momentum: 0.01
      heads:
        energy: 1
    image_loss:
      energy:
        name: reg
        label: labels
        loss: huber
        ignore_value: -1.0
